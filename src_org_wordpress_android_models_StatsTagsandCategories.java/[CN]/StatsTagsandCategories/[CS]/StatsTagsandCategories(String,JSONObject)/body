{
  setBlogId(blogId);
  setTopic(result.getString("topic"));
  if (result.get("type").equals(Type.CATEGORY.getLabel()))   setType(Type.CATEGORY);
 else   setType(Type.TAG);
  setViews(result.getInt("views"));
}
