{
  if (button == 0) {
    if (enabled)     prevPost.setAlpha(160);
 else     prevPost.setAlpha(70);
  }
 else   if (button == 1) {
    if (enabled)     nextPost.setAlpha(160);
 else     nextPost.setAlpha(70);
  }
}
