{
  SharedPreferences sharedPreferences=view.getContext().getSharedPreferences(mPreferencesName,Context.MODE_PRIVATE);
  SharedPreferences.Editor editor=sharedPreferences.edit();
  editor.putString(getViewPathId(view) + mUniqueId,text);
  editor.apply();
}
