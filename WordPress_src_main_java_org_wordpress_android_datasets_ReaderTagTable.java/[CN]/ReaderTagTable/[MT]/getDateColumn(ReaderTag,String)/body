{
  if (tag == null) {
    return "";
  }
  String[] args={tag.getTagName(),Integer.toString(tag.tagType.toInt())};
  return SqlUtils.stringForQuery(ReaderDatabase.getReadableDb(),"SELECT " + colName + " FROM tbl_tag_updates WHERE tag_name=? AND tag_type=?",args);
}
