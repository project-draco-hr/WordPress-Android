{
  final TextView txtFollowCnt=(TextView)findViewById(R.id.text_follow_count);
  String count=getResources().getString(R.string.reader_label_followers,FormatUtils.formatInt(numFollowers));
  txtFollowCnt.setText(count);
}
