{
  if (mActivity == null)   return "";
  if (count > 0) {
    return mActivity.getResources().getQuantityString(R.plurals.site_settings_list_editor_summary,count,count);
  }
  return mActivity.getString(R.string.site_settings_list_editor_no_items_text);
}
