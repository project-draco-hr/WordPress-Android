{
  final CountDownLatch signal=new CountDownLatch(1);
  mClient.requestAccessToken("acode",new Oauth.AccessTokenResponseHandler(){
    @Override public void onPreflight(){
      signal.countDown();
    }
  }
);
  signal.await();
  fail("Did not respond");
}
