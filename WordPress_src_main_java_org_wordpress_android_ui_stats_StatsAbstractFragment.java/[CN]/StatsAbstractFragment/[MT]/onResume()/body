{
  super.onResume();
  if (hasDataAvailable()) {
    updateUI();
  }
 else {
    if (NetworkUtils.isNetworkAvailable(getActivity())) {
      showPlaceholderUI();
      refreshStats();
    }
 else {
      showErrorUI(new NoConnectionError());
    }
  }
}
