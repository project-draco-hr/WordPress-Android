{
  db.beginTransaction();
  try {
    if (clearBeforeSaving)     clearNotes();
    for (    Note note : notes)     addNote(note,false);
    db.setTransactionSuccessful();
  }
  finally {
    db.endTransaction();
  }
}
