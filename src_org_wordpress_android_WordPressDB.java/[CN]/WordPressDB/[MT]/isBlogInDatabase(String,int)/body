{
  Cursor c=db.query(SETTINGS_TABLE,new String[]{"id"},"blogId=? AND url=?",new String[]{String.valueOf(blogId),xmlRpcUrl},null,null,null,null);
  boolean result=c.getCount() > 0;
  c.close();
  return result;
}
