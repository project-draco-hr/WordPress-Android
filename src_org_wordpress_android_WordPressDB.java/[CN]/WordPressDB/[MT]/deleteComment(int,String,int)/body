{
  if (TextUtils.isEmpty(postId))   return false;
  String[] args={Integer.toString(accountId),postId,Integer.toString(commentId)};
  int count=db.delete(COMMENTS_TABLE,"blogID=? AND postID=? AND iCommentID=?",args);
  return (count > 0);
}
