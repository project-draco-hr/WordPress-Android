{
  if (blogName == null || blogURL == null || username == null || password == null)   return false;
  Cursor c=db.query(SETTINGS_TABLE,new String[]{"id","blogName","url"},"blogName=? AND url=? AND username=?",new String[]{blogName,blogURL,username},null,null,null,null);
  int numRows=c.getCount();
  if (numRows > 0) {
    c.moveToFirst();
    long blogID=c.getLong(0);
    ContentValues values=new ContentValues();
    values.put("password",encryptPassword(password));
    db.update(SETTINGS_TABLE,values,"id=" + blogID,null);
    return true;
  }
  c.close();
  return false;
}
