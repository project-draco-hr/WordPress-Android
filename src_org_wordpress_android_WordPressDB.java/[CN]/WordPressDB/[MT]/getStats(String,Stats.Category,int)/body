{
  return db.rawQuery("SELECT * FROM " + STATS_TABLE + " WHERE blogId=? AND category=? AND timeframe=? ORDER BY total DESC, entry ASC",new String[]{blogId,category.name().toLowerCase(),timeframe + ""});
}
