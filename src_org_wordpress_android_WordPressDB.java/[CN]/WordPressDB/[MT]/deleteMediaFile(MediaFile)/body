{
  boolean returnValue=false;
  int result=0;
  result=db.delete(MEDIA_TABLE,"blogId='" + mf.getBlogId() + "' AND id="+ mf.getId(),null);
  if (result == 1) {
    returnValue=true;
  }
  return returnValue;
}
