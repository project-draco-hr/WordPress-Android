{
  if (mNoteJSON == null || mNoteJSON.length() == 0) {
    return;
  }
  if (isCommentType()) {
    mComment=HtmlUtils.fromHtml(getCommentText());
    getCommentPreview();
  }
  getSubject();
  getIconURL();
  getType();
  preloadMetaIds();
}
