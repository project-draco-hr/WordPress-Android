{
  ReaderTagList deletions=new ReaderTagList();
  if (tagList == null) {
    return deletions;
  }
  for (  ReaderTag tag : this) {
    if (indexOfTag(tag) == -1) {
      deletions.add(tag);
    }
  }
  return deletions;
}
