{
  if (tagList == null || tagList.size() != this.size()) {
    return false;
  }
  for (  ReaderTag thisTag : tagList) {
    if (!hasSameTag(thisTag)) {
      return false;
    }
  }
  return true;
}
