{
  if (blogs == null || blogs.size() != this.size()) {
    return false;
  }
  for (  ReaderFollowedBlog blog : blogs) {
    if (indexOfBlogId(blog.blogId) == -1) {
      return false;
    }
  }
  return true;
}
