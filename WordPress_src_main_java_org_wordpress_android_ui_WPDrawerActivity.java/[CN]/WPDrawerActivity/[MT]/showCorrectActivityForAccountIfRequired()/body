{
  if (WordPress.wpDB.getNumVisibleAccounts() == 0) {
    showReader();
    return true;
  }
 else   if (hasNoDotComAccountsAndIsNotOnPostsActivity()) {
    showPosts();
    return true;
  }
 else {
    return false;
  }
}
