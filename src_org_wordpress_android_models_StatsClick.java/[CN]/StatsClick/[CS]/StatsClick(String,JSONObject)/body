{
  setBlogId(blogId);
  setDate(StatUtils.toMs(result.getString("date")));
  setClicks(result.getInt("clicks"));
  setUrl(result.getString("url"));
  if (result.has("imageUrl"))   setImageUrl(result.getString("imageUrl"));
}
