{
  if (tagName == null) {
    return "";
  }
  String sanitized=tagName.replace("&","").replace("#","").replace(" ","-").replace(".","-");
  while (sanitized.contains("--"))   sanitized=sanitized.replace("--","-");
  return sanitized.trim();
}
