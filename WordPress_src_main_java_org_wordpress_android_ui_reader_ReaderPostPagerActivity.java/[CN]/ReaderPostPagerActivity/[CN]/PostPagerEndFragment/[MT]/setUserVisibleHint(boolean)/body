{
  if (Build.VERSION.SDK_INT >= 15) {
    super.setUserVisibleHint(isVisibleToUser);
  }
  if (isAdded() && getView() != null && isVisibleToUser && mFragmentType == EndFragmentType.NO_MORE) {
    animateCheckmark();
  }
}
