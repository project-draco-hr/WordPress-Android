{
  int index=mIdList.indexOf(blogId,postId);
  if (index == -1) {
    return null;
  }
  if (index > 0) {
    return mIdList.get(index - 1);
  }
 else   if (getCount() > 0) {
    return mIdList.get(index + 1);
  }
 else {
    return null;
  }
}
