{
  Fragment fragment=getActiveFragment();
  if (fragment instanceof PostPagerEndFragment) {
    ((PostPagerEndFragment)fragment).setEndFragmentType(canRequestMostPosts() ? EndFragmentType.LOADING : EndFragmentType.NO_MORE);
  }
}
