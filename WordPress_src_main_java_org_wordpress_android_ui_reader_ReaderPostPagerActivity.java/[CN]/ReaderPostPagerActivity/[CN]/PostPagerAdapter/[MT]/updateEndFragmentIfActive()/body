{
  Fragment fragment=getActiveFragment();
  if (fragment instanceof ReaderPostPagerEndFragment) {
    ((ReaderPostPagerEndFragment)fragment).setEndFragmentType(canRequestMostPosts() ? EndFragmentType.LOADING : EndFragmentType.NO_MORE);
  }
}
