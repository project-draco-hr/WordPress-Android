{
  if (isEndFragmentId(mIdList.get(position))) {
    EndFragmentType fragmentType=(canRequestMostPosts() ? EndFragmentType.LOADING : EndFragmentType.NO_MORE);
    PostPagerEndFragment endFragment=PostPagerEndFragment.newInstance(fragmentType);
    return endFragment;
  }
 else {
    return ReaderPostDetailFragment.newInstance(mIdList.get(position).getBlogId(),mIdList.get(position).getPostId(),getPostListType());
  }
}
