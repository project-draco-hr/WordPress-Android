{
  if (!permalink.equals("")) {
    Uri uri=Uri.parse(permalink);
    if (uri != null) {
      Intent i=new Intent(Intent.ACTION_VIEW);
      i.setData(Uri.parse(permalink));
      startActivity(i);
    }
  }
}
