{
  if (result) {
    mTopics=(ReaderTagList)(tmpTopics.clone());
    notifyDataSetChanged();
  }
  mIsTaskRunning=false;
  if (mDataLoadadListener != null)   mDataLoadadListener.onDataLoaded(isEmpty());
}
