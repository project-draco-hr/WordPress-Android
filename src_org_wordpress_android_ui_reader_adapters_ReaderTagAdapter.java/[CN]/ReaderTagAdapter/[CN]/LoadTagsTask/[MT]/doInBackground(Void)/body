{
switch (mTagType) {
case RECOMMENDED:
    tmpTags=ReaderTagTable.getRecommendedTags(true);
  break;
case SUBSCRIBED:
tmpTags=ReaderTagTable.getSubscribedTags();
break;
default :
tmpTags=ReaderTagTable.getDefaultTags();
break;
}
if (tmpTags == null) return false;
if (mTags.isSameList(tmpTags)) return false;
return (tmpTags != null);
}
