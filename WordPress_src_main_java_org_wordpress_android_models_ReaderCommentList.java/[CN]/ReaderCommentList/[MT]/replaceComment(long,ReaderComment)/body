{
  if (newComment == null) {
    return -1;
  }
  int index=indexOfCommentId(commentId);
  if (index == -1) {
    return -1;
  }
  newComment.level=this.get(index).level;
  this.set(index,newComment);
  return index;
}
