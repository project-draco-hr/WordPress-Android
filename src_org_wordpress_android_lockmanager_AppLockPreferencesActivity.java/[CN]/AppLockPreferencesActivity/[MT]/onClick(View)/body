{
  String pinCode=null;
  if (mEnabledCheckBox.isChecked())   pinCode=mPasswordField.getText().toString().trim();
  AppLockManager.getInstance().getCurrentAppLock().setPassword(pinCode);
  finish();
}
