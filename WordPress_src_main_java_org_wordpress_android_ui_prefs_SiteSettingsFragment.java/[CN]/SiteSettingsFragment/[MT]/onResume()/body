{
  super.onResume();
  if (mShouldFetch) {
    new Handler().postDelayed(new Runnable(){
      @Override public void run(){
        mSiteSettings.init(mShouldFetch);
      }
    }
,FETCH_DELAY);
    mShouldFetch=false;
  }
}
