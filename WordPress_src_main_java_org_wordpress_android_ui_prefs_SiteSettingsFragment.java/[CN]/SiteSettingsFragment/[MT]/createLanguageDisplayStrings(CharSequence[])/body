{
  if (languageCodes == null || languageCodes.length < 1)   return null;
  String[] displayStrings=new String[languageCodes.length];
  for (int i=0; i < languageCodes.length; ++i) {
    displayStrings[i]=capitalizeFrontOfString(getLanguageString(String.valueOf(languageCodes[i]),languageLocale(languageCodes[i].toString())),1);
  }
  return displayStrings;
}
