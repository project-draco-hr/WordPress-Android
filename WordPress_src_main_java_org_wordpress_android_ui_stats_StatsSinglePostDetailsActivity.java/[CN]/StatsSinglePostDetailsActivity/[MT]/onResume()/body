{
  super.onResume();
  if (mRestResponseParsed == null) {
    setupEmptyUI(true);
    mHandler.postDelayed(new Runnable(){
      @Override public void run(){
        if (!isFinishing()) {
          refreshStats();
        }
      }
    }
,75L);
  }
 else {
    updateUI();
  }
}
