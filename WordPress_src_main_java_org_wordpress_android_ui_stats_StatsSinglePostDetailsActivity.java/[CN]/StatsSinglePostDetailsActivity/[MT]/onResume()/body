{
  super.onResume();
  mIsInFront=true;
  if (mRestResponseParsed == null) {
    setupEmptyUI(true);
    mHandler.postDelayed(new Runnable(){
      @Override public void run(){
        refreshStats();
      }
    }
,75L);
  }
 else {
    updateUI();
  }
}
