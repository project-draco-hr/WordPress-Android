{
  if (!hasActivity())   return null;
  try {
    Map<Integer,Map<?,?>> commentsResult=ApiHelper.refreshComments(getActivity().getApplicationContext(),commentParams);
    return commentsResult;
  }
 catch (  XMLRPCException e) {
    moderateErrorMsg=e.getLocalizedMessage();
    return null;
  }
}
