{
  if (comment == null || model == null)   return;
  for (int i=0; i < model.size(); i++) {
    Comment thisComment=model.get(i);
    if (thisComment.commentID == comment.commentID) {
      model.set(i,comment);
      getListView().invalidateViews();
      return;
    }
  }
}
