{
  LayoutParams params=(LayoutParams)mFooterSpacer.getLayoutParams();
  if (visible)   params.height=getResources().getDimensionPixelSize(R.dimen.comments_moderation_bar_height);
 else   params.height=0;
  mFooterSpacer.setLayoutParams(params);
}
