{
  return mPost != null && !mIsBlockBlogDisabled && !mPost.isPrivate && !mPost.isExternal && !mIsLoggedOutReader && (mOnPopupListener != null) && (getPostListType() == ReaderPostListType.TAG_FOLLOWED);
}
