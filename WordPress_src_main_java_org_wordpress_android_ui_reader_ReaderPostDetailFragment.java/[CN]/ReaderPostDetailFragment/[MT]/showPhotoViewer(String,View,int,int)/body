{
  if (!isAdded() || TextUtils.isEmpty(imageUrl)) {
    return false;
  }
  if (!imageUrl.startsWith("http")) {
    return false;
  }
  if (hasPost() && mPost.isPrivate) {
    imageUrl=UrlUtils.makeHttps(imageUrl);
  }
  ReaderActivityLauncher.showReaderPhotoViewer(getActivity(),imageUrl,getPostContent(),source,mPost.isPrivate || ReaderBlogTable.getBlogInfo(mBlogId,null).isPrivate,startX,startY);
  return true;
}
