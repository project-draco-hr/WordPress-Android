{
  if (!isAdded() || TextUtils.isEmpty(imageUrl)) {
    return false;
  }
  if (!imageUrl.startsWith("http")) {
    return false;
  }
  if (hasPost() && mPost.isPrivate) {
    imageUrl=UrlUtils.makeHttps(imageUrl);
  }
  ReaderActivityLauncher.showReaderPhotoViewer(getActivity(),imageUrl,mPost.getText(),source,startX,startY);
  return true;
}
