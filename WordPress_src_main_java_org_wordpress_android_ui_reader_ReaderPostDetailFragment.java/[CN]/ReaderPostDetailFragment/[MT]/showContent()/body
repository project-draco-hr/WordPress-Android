{
  if (!isAdded()) {
    return;
  }
  if (mNote != null) {
    mReaderWebView.setVisibility(View.VISIBLE);
  }
  refreshLikes();
  refreshComments();
  if (!mHasAlreadyUpdatedPost) {
    updatePost();
    mHasAlreadyUpdatedPost=true;
  }
}
