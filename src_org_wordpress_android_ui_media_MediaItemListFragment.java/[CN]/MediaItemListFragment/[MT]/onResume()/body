{
  super.onResume();
  loadCursor();
  mAdapter=new MediaItemListAdapter(getActivity(),mCursor,0);
  setListAdapter(mAdapter);
  if (WordPress.getCurrentBlog() != null)   refreshMediaFromServer();
}
