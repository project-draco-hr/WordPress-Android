{
  super.onResume();
  Log.d("WordPress","MediaItemListFragment onResume()");
  if (WordPress.getCurrentBlog() != null)   refreshMedia();
  loadCursor();
  mAdapter=new MediaCursorAdapter(getActivity(),mCursor,0);
  setListAdapter(mAdapter);
}
