{
  int bytes=0;
  if (Build.VERSION.SDK_INT < Build.VERSION_CODES.HONEYCOMB_MR1) {
    bytes=(value.getRowBytes() * value.getHeight());
  }
 else {
    bytes=value.getByteCount();
  }
  if (bytes != 0)   bytes=bytes / 1024;
  return bytes;
}
