{
  if (MediaUtils.isPicasaImage(imageUri)) {
    new DownloadImageTask().execute(imageUri);
  }
 else {
    if (!addMedia(imageUri,null))     Toast.makeText(getActivity(),getResources().getText(R.string.gallery_error),Toast.LENGTH_SHORT).show();
  }
}
