{
  StatsTimeframe timeframe=StatsTimeframe.DAYS;
  if (position == 1)   timeframe=StatsTimeframe.WEEKS;
 else   if (position == 2)   timeframe=StatsTimeframe.MONTHS;
  return InnerFragment.newInstance(timeframe);
}
