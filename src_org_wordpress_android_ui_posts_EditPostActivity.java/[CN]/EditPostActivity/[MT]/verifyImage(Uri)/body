{
  if (isPicasaImage(imageUri)) {
    new DownloadImageTask().execute(imageUri);
  }
 else {
    if (!addMedia(imageUri,null))     Toast.makeText(EditPostActivity.this,getResources().getText(R.string.gallery_error),Toast.LENGTH_SHORT).show();
  }
}
