{
  mHandler.postDelayed(new Runnable(){
    @Override public void run(){
      if (mWebView.getVisibility() != View.VISIBLE)       AppLog.d(T.READER,"reader post detail > webView shown before page finished");
      showContent();
    }
  }
,750L);
}
