{
switch (item.getItemId()) {
case R.id.menu_browse:
    if (hasPost())     ReaderActivityLauncher.openUrl(getActivity(),mPost.getUrl(),OpenUrlType.EXTERNAL);
  return true;
case R.id.menu_share:
sharePage();
return true;
default :
return super.onOptionsItemSelected(item);
}
}
