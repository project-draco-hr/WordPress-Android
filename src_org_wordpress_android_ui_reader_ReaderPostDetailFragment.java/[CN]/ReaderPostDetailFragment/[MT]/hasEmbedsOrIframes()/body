{
  if (mPost == null) {
    return false;
  }
 else   if (mPost.isVideoPress || mPost.hasFeaturedVideo()) {
    return true;
  }
 else {
    return (mPost.getText().contains("<embed") || mPost.getText().contains("<iframe") || mPost.getText().contains("videos.files.wordpress.com"));
  }
}
