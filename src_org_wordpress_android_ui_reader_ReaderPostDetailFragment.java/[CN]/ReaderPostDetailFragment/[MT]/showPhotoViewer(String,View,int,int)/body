{
  if (!hasActivity() || TextUtils.isEmpty(imageUrl)) {
    return false;
  }
  boolean isPrivate=hasPost() && mPost.isPrivate;
  ReaderActivityLauncher.showReaderPhotoViewer(getActivity(),imageUrl,isPrivate,source,startX,startY);
  return true;
}
