{
  if (TextUtils.isEmpty(url))   return url;
  if (url.contains("d=identicon"))   return url.replace("d=identicon","d=mm");
  String prefix=(url.contains("?") ? "&" : "?");
  return url + prefix + "d=mm";
}
