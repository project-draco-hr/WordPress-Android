{
  if (TextUtils.isEmpty(email))   return "";
  String url="http://gravatar.com/avatar/" + StringUtils.getMd5Hash(email) + "?d=mm";
  if (size > 0)   url+="&s=" + Integer.toString(size);
  return url;
}
