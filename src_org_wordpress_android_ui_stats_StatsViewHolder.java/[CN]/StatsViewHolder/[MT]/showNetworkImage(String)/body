{
  if (networkImageView == null)   return;
  if (imageUrl != null && imageUrl.startsWith("http")) {
    networkImageView.setImageUrl(imageUrl,WordPress.imageLoader);
  }
 else {
    networkImageView.setImageResource(R.drawable.stats_blank_image);
  }
}
