{
  if (jsonObject == null) {
    if (infoListener != null)     infoListener.onResult(null);
    return;
  }
  ReaderBlogInfo blogInfo=ReaderBlogInfo.fromJson(jsonObject);
  ReaderBlogTable.setBlogInfo(blogInfo);
  if (infoListener != null)   infoListener.onResult(blogInfo);
}
