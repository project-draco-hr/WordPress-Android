{
  boolean isCommentXPost=post.getExcerpt().startsWith("X-comment");
  String subtitle=String.format(isCommentXPost ? FMT_COMMENT_XPOST : FMT_SITE_XPOST,"<strong>" + post.getAuthorName() + "</strong>",getFromSiteName(post),getToSiteName(post));
  return Html.fromHtml(subtitle);
}
