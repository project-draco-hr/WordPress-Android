{
  if (firstTag == null) {
    String allTags=getTags();
    int pos=allTags.indexOf(",");
    if (pos > 0) {
      firstTag=allTags.substring(0,pos);
    }
 else {
      firstTag=allTags;
    }
  }
  return firstTag;
}
