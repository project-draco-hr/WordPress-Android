{
  if (i == KeyEvent.KEYCODE_BACK) {
    if (loadReader) {
      if (wv.canGoBack() && !wv.getUrl().equals("http://en.wordpress.com/reader/mobile/?preload=false")) {
        wv.goBack();
      }
 else {
        finish();
      }
    }
 else {
      finish();
    }
  }
  return false;
}
