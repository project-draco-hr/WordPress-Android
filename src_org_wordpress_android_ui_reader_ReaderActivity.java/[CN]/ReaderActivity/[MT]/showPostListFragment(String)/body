{
  String tagForFragment=ReaderFragmentType.POST_LIST.toString();
  Fragment fragment=ReaderPostListFragment.newInstance(tagName);
  FragmentTransaction ft=getSupportFragmentManager().beginTransaction().add(R.id.fragment_container,fragment,tagForFragment);
  ft.commit();
}
