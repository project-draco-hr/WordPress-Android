{
  if (mRefreshMenuItem == null || isUpdating == mIsUpdating)   return;
  mIsUpdating=isUpdating;
  if (isUpdating) {
    startAnimatingRefreshButton(mRefreshMenuItem);
  }
 else {
    stopAnimatingRefreshButton(mRefreshMenuItem);
  }
}
