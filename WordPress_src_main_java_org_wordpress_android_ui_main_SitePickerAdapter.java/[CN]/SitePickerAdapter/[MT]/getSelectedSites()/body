{
  SitePickerActivity.SiteList sites=new SitePickerActivity.SiteList();
  if (!mEnableSelection)   return sites;
  for (  Integer position : mSelectedPositions) {
    if (isValidPosition(position))     sites.add(mSites.get(position));
  }
  return sites;
}
