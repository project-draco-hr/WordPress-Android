{
  if (mRestoredListPosition != ListView.INVALID_POSITION && mRestoredListPosition < mNotesAdapter.getCount()) {
    getListView().setSelectionFromTop(mRestoredListPosition,0);
    mRestoredListPosition=ListView.INVALID_POSITION;
  }
}
