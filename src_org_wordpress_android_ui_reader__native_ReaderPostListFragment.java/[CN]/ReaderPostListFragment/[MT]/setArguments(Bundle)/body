{
  super.setArguments(args);
  if (args != null) {
    if (args.containsKey(KEY_TOPIC_NAME))     mCurrentTopic=args.getString(KEY_TOPIC_NAME);
    mIsTranslucentActionBarEnabled=args.getBoolean(KEY_TRANSLUCENT_ACTION_BAR);
  }
}
