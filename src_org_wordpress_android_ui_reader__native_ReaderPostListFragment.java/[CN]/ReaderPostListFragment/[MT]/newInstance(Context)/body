{
  ReaderLog.d("post list newInstance");
  String topicName=ReaderPrefs.getReaderTopic();
  if (TextUtils.isEmpty(topicName) || !ReaderTagTable.tagExists(topicName))   topicName=context.getString(R.string.reader_default_tag_name);
  Bundle args=new Bundle();
  args.putString(KEY_TOPIC_NAME,topicName);
  ReaderPostListFragment fragment=new ReaderPostListFragment();
  fragment.setArguments(args);
  return fragment;
}
