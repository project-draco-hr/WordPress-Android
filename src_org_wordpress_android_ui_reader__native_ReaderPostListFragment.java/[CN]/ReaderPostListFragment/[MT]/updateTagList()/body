{
  ReaderActions.UpdateResultListener listener=new ReaderActions.UpdateResultListener(){
    @Override public void onUpdateResult(    ReaderActions.UpdateResult result){
      if (!hasActivity()) {
        ReaderLog.w("volley response when fragment has no activity");
        return;
      }
      if (result != ReaderActions.UpdateResult.FAILED)       mAlreadyUpdatedTagList=true;
      if (result == ReaderActions.UpdateResult.CHANGED)       refreshTags();
    }
  }
;
  ReaderTagActions.updateTags(listener);
}
