{
switch (getBlogType()) {
case RECOMMENDED:
    if (mRecommendedBlogs.size() == 0) {
      return 0;
    }
 else {
      return mRecommendedBlogs.size() + 1;
    }
case FOLLOWED:
  return mFollowedBlogs.size();
default :
return 0;
}
}
