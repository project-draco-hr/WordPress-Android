{
switch (getBlogType()) {
case RECOMMENDED:
    if (position < mRecommendedBlogs.size()) {
      return VIEW_TYPE_ITEM;
    }
 else {
      return VIEW_TYPE_FOOTER;
    }
default :
  return VIEW_TYPE_ITEM;
}
}
