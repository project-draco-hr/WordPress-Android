{
  int pos=-1;
  if (tm != null && mFilterValues != null) {
    for (int i=0; i < mFilterValues.length; i++) {
      FilterCriteria obj=mFilterValues[i];
      if (obj.equals(tm)) {
        pos=i;
        return pos;
      }
    }
  }
  return pos;
}
