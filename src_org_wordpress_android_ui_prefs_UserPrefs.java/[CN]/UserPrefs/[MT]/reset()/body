{
  prefs().edit().remove(PREFKEY_USER_ID).remove(PREFKEY_READER_TAG_NAME).remove(PREFKEY_READER_TAG_TYPE).remove(PREFKEY_READER_RECOMMENDED_OFFSET).remove(PREFKEY_READER_SUBS_PAGE_TITLE).commit();
}
