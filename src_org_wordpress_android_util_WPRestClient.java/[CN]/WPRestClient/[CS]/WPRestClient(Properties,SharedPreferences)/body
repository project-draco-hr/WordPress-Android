{
  mPrefs=prefs;
  mOauth=new Oauth(config.getProperty(APP_ID_PROPERTY),config.getProperty(APP_SECRET_PROPERTY),config.getProperty(APP_REDIRECT_PROPERTY));
  mRestClient=new RestClient(prefs.getString(ACCESS_TOKEN_PREFERNCE,null));
}
