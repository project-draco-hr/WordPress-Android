{
  if (mNotesBucket != null) {
    mNotesBucket.reset();
  }
  if (mMetaBucket != null) {
    mMetaBucket.reset();
  }
  if (mSimperium != null) {
    mSimperium.getUser().setStatus(User.Status.UNKNOWN);
  }
}
