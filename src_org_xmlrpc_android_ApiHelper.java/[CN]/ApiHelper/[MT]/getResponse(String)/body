{
  HttpRequest request=getHttpRequest(urlString);
  if (request != null) {
    try {
      String body=request.body();
      return body;
    }
 catch (    HttpRequestException e) {
      return null;
    }
  }
 else {
    return null;
  }
}
