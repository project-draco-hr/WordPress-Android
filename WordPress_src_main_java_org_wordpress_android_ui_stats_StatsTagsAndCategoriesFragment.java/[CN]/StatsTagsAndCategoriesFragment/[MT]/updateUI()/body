{
  if (mDatamodels != null && mDatamodels[0] != null && ((TagsContainerModel)mDatamodels[0]).getTags() != null && (((TagsContainerModel)mDatamodels[0]).getTags()).size() > 0) {
    BaseExpandableListAdapter adapter=new MyExpandableListAdapter(getActivity(),((TagsContainerModel)mDatamodels[0]).getTags());
    StatsUIHelper.reloadGroupViews(getActivity(),adapter,mGroupIdToExpandedMap,mList,getMaxNumberOfItemsToShowInList());
    showEmptyUI(false);
  }
 else {
    showEmptyUI(true);
  }
}
