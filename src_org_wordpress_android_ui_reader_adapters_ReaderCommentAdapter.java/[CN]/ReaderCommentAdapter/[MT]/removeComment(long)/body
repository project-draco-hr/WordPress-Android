{
  if (commentId == mHighlightCommentId)   setHighlightCommentId(0,false);
  int position=indexOfCommentId(commentId);
  if (position == -1)   return;
  mComments.remove(position);
  notifyDataSetChanged();
}
