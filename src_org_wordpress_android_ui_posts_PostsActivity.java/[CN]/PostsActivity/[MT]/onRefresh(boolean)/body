{
  if (start) {
    attemptToSelectPost();
    startAnimatingRefreshButton(refreshMenuItem);
    isRefreshing=true;
  }
 else {
    stopAnimatingRefreshButton(refreshMenuItem);
    isRefreshing=false;
  }
}
