{
  if (requestCode == ACTIVITY_EDIT_POST && resultCode == RESULT_OK && data != null) {
    if (data.getBooleanExtra("shouldRefresh",false))     postList.loadPosts(false);
  }
  super.onActivityResult(requestCode,resultCode,data);
}
