{
  FragmentManager fm=getSupportFragmentManager();
  if (isInMultiSelect()) {
    cancelMultiSelect();
  }
 else   if (fm.getBackStackEntryCount() > 0) {
    fm.popBackStack();
    setupBaseLayout();
  }
 else {
    super.onBackPressed();
  }
}
