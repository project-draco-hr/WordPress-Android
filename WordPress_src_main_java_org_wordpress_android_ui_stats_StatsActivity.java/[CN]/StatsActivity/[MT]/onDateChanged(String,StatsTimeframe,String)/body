{
  if (isFinishing() || isActivityDestroyed()) {
    return;
  }
  mRequestedDate=date;
  refreshStats(timeframe,date,false,false);
  emptyDataModelInFragments(false,false);
  loadStatsFragments(false,false,false);
}
