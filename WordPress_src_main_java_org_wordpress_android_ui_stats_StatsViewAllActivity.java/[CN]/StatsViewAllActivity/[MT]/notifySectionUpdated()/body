{
  Intent intent=new Intent().setAction(StatsService.ACTION_STATS_UPDATED).putExtra(StatsService.EXTRA_ENDPOINT_NAME,getRestEndpointName()).putExtra(StatsService.EXTRA_ENDPOINT_DATA,mRestResponse);
  LocalBroadcastManager.getInstance(WordPress.getContext()).sendBroadcast(intent);
}
