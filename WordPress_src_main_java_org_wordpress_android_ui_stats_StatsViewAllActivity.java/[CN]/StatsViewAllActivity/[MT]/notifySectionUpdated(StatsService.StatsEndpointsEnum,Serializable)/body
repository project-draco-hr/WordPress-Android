{
  Intent intent=new Intent().setAction(StatsService.ACTION_STATS_SECTION_UPDATED).putExtra(StatsService.EXTRA_ENDPOINT_NAME,sectionName).putExtra(StatsService.EXTRA_ENDPOINT_DATA,data);
  LocalBroadcastManager.getInstance(WordPress.getContext()).sendBroadcast(intent);
}
