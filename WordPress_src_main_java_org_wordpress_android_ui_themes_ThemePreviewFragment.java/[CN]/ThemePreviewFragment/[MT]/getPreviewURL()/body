{
  if (mPreviewURL != null) {
    return mPreviewURL;
  }
 else   if (getArguments() != null) {
    mPreviewURL=getArguments().getString(ARGS_PREVIEW_URL);
    return mPreviewURL;
  }
 else {
    return null;
  }
}
