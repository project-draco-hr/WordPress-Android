{
  if (!isAdded())   return;
  setIsUpdating(false);
  if (event.hasResults()) {
    getPostAdapter().setCurrentSearchQuery(event.getQuery());
  }
 else {
    mDataLoadedListener.onDataLoaded(true);
  }
}
