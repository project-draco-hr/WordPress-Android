{
  super.onResume();
  if (mWasPaused) {
    AppLog.d(T.READER,"reader post list > resumed from paused state");
    mWasPaused=false;
    refreshPosts();
    refreshTags();
    updateCurrentTagIfTime();
  }
}
