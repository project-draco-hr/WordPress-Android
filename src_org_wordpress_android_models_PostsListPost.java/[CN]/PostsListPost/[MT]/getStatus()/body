{
  Date d=new Date();
  String postStatus=StringUtils.notNullStr(status);
  if (getDateCreatedGmt() - TEN_SECONDS_MILLIS > d.getTime() && PostStatus.fromString(getOriginalStatus()) == PostStatus.PUBLISHED) {
    postStatus=PostStatus.toString(PostStatus.SCHEDULED);
  }
  return postStatus;
}
