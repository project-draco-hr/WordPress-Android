{
  if (bitmap != null) {
    hideProgress();
    mImageView.setImageBitmap(bitmap);
    setAttacher();
    if (isLoRes && !mLoResImageUrl.equals(mHiResImageUrl)) {
      loadHiResImage();
    }
  }
}
