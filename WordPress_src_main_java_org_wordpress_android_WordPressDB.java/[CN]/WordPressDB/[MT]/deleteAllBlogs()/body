{
  List<Integer> ids=getAllBlogsIDs();
  if (ids.size() == 0)   return;
  db.beginTransaction();
  try {
    for (    int id : ids) {
      deleteBlog(context,id);
    }
    db.setTransactionSuccessful();
  }
  finally {
    db.endTransaction();
  }
}
