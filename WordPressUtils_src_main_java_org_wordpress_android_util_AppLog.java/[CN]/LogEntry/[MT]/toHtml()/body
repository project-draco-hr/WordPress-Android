{
  StringBuilder sb=new StringBuilder().append("<font color=\"").append(logLevel.toHtmlColor()).append("\">").append("[").append(logTag.name()).append("] ").append(logLevel.name()).append(": ").append(TextUtils.htmlEncode(logText).replace("\n","<br />")).append("</font>");
  return sb.toString();
}
