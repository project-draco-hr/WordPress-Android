{
  currentTask=null;
  titleBar.stopRotatingRefreshIcon();
  if (authed) {
    loadStats();
  }
 else {
    if (errorMsg == "" || statusCode == 401) {
      Toast.makeText(ViewWebStats.this,getResources().getText(R.string.invalid_login),Toast.LENGTH_SHORT).show();
      showLoginForm();
    }
 else {
      Toast.makeText(ViewWebStats.this,getResources().getText(R.string.stats_service_error),Toast.LENGTH_SHORT).show();
    }
  }
}
