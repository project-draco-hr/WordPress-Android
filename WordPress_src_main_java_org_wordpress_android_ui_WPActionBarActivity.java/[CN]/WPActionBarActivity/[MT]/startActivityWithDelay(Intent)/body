{
  if (isXLargeLandscape()) {
    startActivity(i);
  }
 else {
    if (mFirstLaunch) {
      startActivity(i);
      return;
    }
    new Handler().postDelayed(new Runnable(){
      @Override public void run(){
        startActivity(i);
      }
    }
,400);
  }
}
