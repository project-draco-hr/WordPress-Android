{
  if (!isGooglePlayServicesAvailable()) {
    return;
  }
  mCredentialsClient=new GoogleApiClient.Builder(getActivity()).addConnectionCallbacks(this).enableAutoManage((SignInActivity)getActivity(),this).addApi(Auth.CREDENTIALS_API).build();
}
