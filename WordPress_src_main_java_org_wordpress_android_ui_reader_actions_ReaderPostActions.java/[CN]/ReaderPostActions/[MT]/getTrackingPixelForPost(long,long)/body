{
  String blogUrl=ReaderPostTable.getPostBlogUrl(blogId,postId);
  if (TextUtils.isEmpty(blogUrl)) {
    return null;
  }
  return "https://pixel.wp.com/g.gif?v=wpcom&reader=1" + "&blog=" + blogId + "&post="+ postId+ "&host="+ UrlUtils.urlEncode(UrlUtils.getDomainFromUrl(blogUrl))+ "&ref="+ UrlUtils.urlEncode(TRACKING_REFERRER)+ "&t="+ new Random().nextInt();
}
