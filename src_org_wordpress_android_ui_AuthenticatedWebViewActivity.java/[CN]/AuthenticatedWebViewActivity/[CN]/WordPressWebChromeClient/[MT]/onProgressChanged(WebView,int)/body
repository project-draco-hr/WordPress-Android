{
  AuthenticatedWebViewActivity.this.setTitle(context.getResources().getText(R.string.loading));
  AuthenticatedWebViewActivity.this.setSupportProgress(progress * 100);
  if (progress == 100) {
    setTitle(webView.getTitle());
  }
}
