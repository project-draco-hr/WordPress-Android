{
  CommentDetailFragment commentFragment=CommentDetailFragment.newInstance(note);
  String tagForFragment=getString(R.string.fragment_tag_comment_detail);
  FragmentTransaction ft=getSupportFragmentManager().beginTransaction();
  FragmentUtils.setTransition(ft);
  ft.replace(R.id.layout_fragment_container,commentFragment,tagForFragment).addToBackStack(tagForFragment).commit();
}
