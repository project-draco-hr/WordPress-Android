{
  if (WordPress.latestNotes == null)   return false;
  try {
    tmpNotes=parseNotes(WordPress.latestNotes);
    return (tmpNotes != null);
  }
 catch (  JSONException e) {
    Log.e(TAG,"No cached notes");
    return false;
  }
}
