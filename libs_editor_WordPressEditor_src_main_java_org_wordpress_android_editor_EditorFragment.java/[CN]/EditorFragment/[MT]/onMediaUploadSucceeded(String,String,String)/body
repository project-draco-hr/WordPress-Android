{
  mWebView.post(new Runnable(){
    @Override public void run(){
      mWebView.execJavaScriptFromString("ZSSEditor.replaceLocalImageWithRemoteImage(" + mediaId + ", '"+ remoteId+ "', '"+ remoteUrl+ "');");
      mUploadingMediaIds.remove(mediaId);
    }
  }
);
}
