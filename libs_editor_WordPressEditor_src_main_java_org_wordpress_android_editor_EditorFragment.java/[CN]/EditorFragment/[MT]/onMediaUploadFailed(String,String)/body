{
  mWebView.post(new Runnable(){
    @Override public void run(){
      mWebView.execJavaScriptFromString("ZSSEditor.markImageUploadFailed(" + mediaId + ", '"+ errorMessage+ "');");
      mFailedMediaIds.add(mediaId);
      mUploadingMediaIds.remove(mediaId);
    }
  }
);
}
