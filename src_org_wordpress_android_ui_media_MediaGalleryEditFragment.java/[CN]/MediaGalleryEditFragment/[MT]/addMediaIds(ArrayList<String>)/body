{
  if (ids == null)   return;
  if (mIds == null) {
    mIds=ids;
  }
 else {
    for (    String currentID : ids) {
      if (!mIds.contains(currentID))       mIds.add(currentID);
    }
  }
  refreshGridView();
}
