{
  String ids="";
  if (mIds.size() > 0) {
    ids="ids=\"";
    for (    String id : mIds) {
      ids+=id + ",";
    }
    ids=ids.substring(0,ids.length() - 1);
    ids+="\"";
  }
  return ids;
}
