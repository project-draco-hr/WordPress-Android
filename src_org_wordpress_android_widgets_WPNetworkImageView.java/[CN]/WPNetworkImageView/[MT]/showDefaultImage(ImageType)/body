{
switch (imageType) {
case PHOTO_FULL:
    setImageDrawable(null);
  break;
case MSHOT:
setImageDrawable(null);
break;
default :
setImageDrawable(new ColorDrawable(getColorRes(R.color.grey_light)));
break;
}
}
