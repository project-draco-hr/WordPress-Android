{
  if (mThemeId != null) {
    return mThemeId;
  }
 else   if (getArguments() != null) {
    mThemeId=getArguments().getString(ARGS_THEME_ID);
    return mThemeId;
  }
 else {
    return null;
  }
}
