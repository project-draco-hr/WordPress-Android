{
  if (TextUtils.isEmpty(text))   return text;
  if (text.lastIndexOf("<p") > 0)   text=text.replaceAll("<p(.|\n)*?>","\n<p>");
  if (text.contains("<br"))   text=text.replaceAll("<br(.|\n)*?>","\n");
  return fastUnescapeHtml(text.replaceAll("<(.|\n)*?>","").trim());
}
