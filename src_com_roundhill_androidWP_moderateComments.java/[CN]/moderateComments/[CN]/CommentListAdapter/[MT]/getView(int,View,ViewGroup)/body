{
  CommentView cv;
  if (convertView == null) {
    cv=new CommentView(mContext,authors[position],comments[position],status[position],commentID[position]);
  }
 else {
    cv=(CommentView)convertView;
    cv.setAuthor(authors[position]);
    cv.setComment(comments[position]);
    cv.setCommentID(commentID[position]);
  }
  changedComments.clear();
  return cv;
}
