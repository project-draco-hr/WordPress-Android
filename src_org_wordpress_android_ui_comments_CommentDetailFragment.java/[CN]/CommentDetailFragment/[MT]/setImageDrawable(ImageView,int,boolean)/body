{
  Drawable drawable=getResources().getDrawable(resId).mutate();
  if (!isEnabled)   drawable.setColorFilter(getResources().getColor(R.color.grey_medium),PorterDuff.Mode.SRC_ATOP);
  view.setImageDrawable(drawable);
}
