{
  if (!mIsCommentBoxShowing)   return;
  if (!hasActivity())   return;
  ViewGroup layoutComment=(ViewGroup)getActivity().findViewById(R.id.layout_comment_box);
  ReaderAniUtils.flyOut(layoutComment);
  mIsCommentBoxShowing=false;
}
