{
  mComment=comment;
  mBlogId=blogId;
  mAccountId=WordPress.wpDB.getAccountIdForBlogId(blogId);
  mIsUsersBlog=(comment != null && WordPress.wpDB.isBlogIdInDatabase(mBlogId));
  if (hasActivity())   showComment();
}
