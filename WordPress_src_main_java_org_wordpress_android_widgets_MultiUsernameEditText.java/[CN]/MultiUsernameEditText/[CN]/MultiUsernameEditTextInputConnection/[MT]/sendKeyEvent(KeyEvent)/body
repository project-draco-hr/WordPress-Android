{
  if (event.getAction() == KeyEvent.ACTION_DOWN && event.getKeyCode() == KeyEvent.KEYCODE_DEL) {
    if (mOnBackspacePressedListener != null)     mOnBackspacePressedListener.onBackspacePressed();
  }
  return super.sendKeyEvent(event);
}
