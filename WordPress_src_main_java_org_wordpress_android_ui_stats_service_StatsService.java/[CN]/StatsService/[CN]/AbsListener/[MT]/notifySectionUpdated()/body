{
  Intent intent=new Intent().setAction(ACTION_ONE_STAT_UPDATED).putExtra(EXTRA_UPDATED_SECTION,getSectionEnum());
  LocalBroadcastManager.getInstance(WordPress.getContext()).sendBroadcast(intent);
}
