{
  Intent intent=new Intent().setAction(ACTION_STATS_UPDATING).putExtra(EXTRA_ENDPOINT_NAME,sectionName).putExtra(EXTRA_IS_UPDATING,true);
  LocalBroadcastManager.getInstance(this).sendBroadcast(intent);
}
