{
  if (!NetworkUtils.isNetworkAvailable(this)) {
    return;
  }
  ReaderUpdateService.startService(this,EnumSet.of(UpdateTask.TAGS,UpdateTask.FOLLOWED_BLOGS,UpdateTask.RECOMMENDED_BLOGS));
  mHasPerformedUpdate=true;
}
