{
  if (y < 0)   return 0;
  if (y > mHeight)   return Integer.MAX_VALUE;
  y+=getScrollY();
  for (int i=0; i < coords.size(); i++) {
    if (coords.get(i).contains(x,y))     return i;
  }
  return 0;
}
