{
  int[] pos=getLocationOnScreen();
  if (oldPos < 0)   return new Point(pos[0],pos[1]);
 else   if (oldPos >= coords.size())   return new Point(pos[0],pos[1] + getHeight());
  Rect rect=coords.get(oldPos);
  return new Point(rect.left,rect.top);
}
