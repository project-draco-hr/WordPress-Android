{
  super.onResume();
  if (mVisitsData != null) {
    updateUI();
  }
 else {
    if (NetworkUtils.isNetworkAvailable(getActivity())) {
      setupNoResultsUI(true);
      refreshStats();
    }
 else {
      setupNoResultsUI(false);
    }
  }
}
