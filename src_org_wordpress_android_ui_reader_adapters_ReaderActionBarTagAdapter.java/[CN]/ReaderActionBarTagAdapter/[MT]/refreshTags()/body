{
  if (mIsTaskRunning)   AppLog.w("Load tags task already running");
  if (SysUtils.canUseExecuteOnExecutor()) {
    new LoadTagsTask().executeOnExecutor(AsyncTask.THREAD_POOL_EXECUTOR);
  }
 else {
    new LoadTagsTask().execute();
  }
}
