{
  if (newUri != null) {
    addMedia(newUri,null,EditPostActivity.this);
  }
 else {
    Toast.makeText(EditPostActivity.this,getString(R.string.error_downloading_image),Toast.LENGTH_SHORT).show();
  }
}
