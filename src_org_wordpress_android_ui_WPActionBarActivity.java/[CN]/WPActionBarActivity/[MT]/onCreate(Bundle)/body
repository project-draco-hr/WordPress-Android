{
  super.onCreate(savedInstanceState);
  if (this instanceof WebViewActivity)   requestWindowFeature(Window.FEATURE_PROGRESS);
  getSupportActionBar().setDisplayShowTitleEnabled(false);
  if ((getResources().getConfiguration().screenLayout & Configuration.SCREENLAYOUT_SIZE_MASK) == 4)   mIsXLargeDevice=true;
}
