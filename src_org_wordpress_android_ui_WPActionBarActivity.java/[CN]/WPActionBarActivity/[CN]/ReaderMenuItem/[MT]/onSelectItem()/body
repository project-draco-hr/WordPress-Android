{
  if (!isSelected())   mShouldFinish=true;
  Intent intent;
  intent=new Intent(WPActionBarActivity.this,ReaderActivity.class);
  intent.setFlags(Intent.FLAG_ACTIVITY_NO_ANIMATION);
  startActivityWithDelay(intent);
}
