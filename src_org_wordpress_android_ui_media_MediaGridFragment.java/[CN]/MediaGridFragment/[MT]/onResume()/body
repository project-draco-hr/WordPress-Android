{
  super.onResume();
  mPullToRefreshHelper.registerReceiver(getActivity());
  if (!NetworkUtils.isNetworkAvailable(this.getActivity()))   mHasRetrievedAllMedia=true;
  refreshSpinnerAdapter();
  refreshMediaFromDB();
}
