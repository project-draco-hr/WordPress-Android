{
  if (!hasActivity() || mIsUpdating == isUpdating)   return;
  mIsUpdating=isUpdating;
switch (updateAction) {
case LOAD_NEWER:
    animateRefreshButton(isUpdating);
  break;
case LOAD_OLDER:
if (isUpdating) {
  showLoadingProgress();
}
 else {
  hideLoadingProgress();
}
break;
}
}
