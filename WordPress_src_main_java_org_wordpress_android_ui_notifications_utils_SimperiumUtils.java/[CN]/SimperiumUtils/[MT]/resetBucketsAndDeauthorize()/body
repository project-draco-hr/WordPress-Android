{
  if (mNotesBucket != null) {
    mNotesBucket.reset();
    mNotesBucket=null;
  }
  if (mMetaBucket != null) {
    mMetaBucket.reset();
    mMetaBucket=null;
  }
  if (mSimperium != null) {
    mSimperium.getUser().setStatus(User.Status.UNKNOWN);
    mSimperium=null;
  }
}
