{
  Person person=loadPerson();
  if (person == null) {
    return;
  }
  RoleChangeDialogFragment.show((PeopleManagementActivity)getActivity(),person.getPersonID(),person.getLocalTableBlogId(),person.getRole());
}
