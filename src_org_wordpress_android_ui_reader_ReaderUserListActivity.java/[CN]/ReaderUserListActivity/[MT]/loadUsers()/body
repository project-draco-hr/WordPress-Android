{
  if (mIsTaskRunning)   AppLog.w(AppLog.T.READER,"user task already running");
  if (SysUtils.canUseExecuteOnExecutor()) {
    new LoadUsersTask().executeOnExecutor(AsyncTask.THREAD_POOL_EXECUTOR);
  }
 else {
    new LoadUsersTask().execute();
  }
}
