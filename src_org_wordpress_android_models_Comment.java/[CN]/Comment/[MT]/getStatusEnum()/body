{
  if (status == null)   return CommentStatus.UNKNOWN;
  if (status.equals("approve"))   return CommentStatus.APPROVED;
  if (status.equals("hold"))   return CommentStatus.PENDING;
  return CommentStatus.UNKNOWN;
}
