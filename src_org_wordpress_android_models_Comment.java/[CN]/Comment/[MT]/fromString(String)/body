{
  if (value == null)   return CommentStatus.UNKNOWN;
  if (value.equals("approve"))   return CommentStatus.APPROVED;
  if (value.equals("hold"))   return CommentStatus.PENDING;
  if (value.equals("spam"))   return SPAM;
  return CommentStatus.UNKNOWN;
}
