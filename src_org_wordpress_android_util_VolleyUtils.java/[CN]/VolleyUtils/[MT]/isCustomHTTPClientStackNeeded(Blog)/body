{
  if (currentBlog.hasValidHTTPAuthCredentials())   return true;
  String domain=UrlUtils.getDomainFromUrl(currentBlog.getUrl());
  if (TrustedSslDomainTable.isDomainTrusted(domain)) {
    return true;
  }
  return false;
}
