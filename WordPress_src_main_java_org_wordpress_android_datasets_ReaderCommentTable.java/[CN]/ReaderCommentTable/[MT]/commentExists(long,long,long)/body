{
  String[] args={Long.toString(blogId),Long.toString(postId),Long.toString(commentId)};
  return SqlUtils.longForQuery(ReaderDatabase.getReadableDb(),"SELECT blog_id FROM tbl_comments WHERE blog_id=? AND post_id=? and comment_id=?",args) > 0;
}
