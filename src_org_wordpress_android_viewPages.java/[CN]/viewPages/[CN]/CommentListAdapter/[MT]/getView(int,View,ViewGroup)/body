{
  CommentView cv;
  if (convertView == null) {
    cv=new CommentView(mContext,titles[position],dateCreatedFormatted[position]);
  }
 else {
    cv=(CommentView)convertView;
    cv.setTitle(titles[position]);
    cv.setDate(dateCreatedFormatted[position]);
  }
  return cv;
}
