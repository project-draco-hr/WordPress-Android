{
  if (entryTextView == null || linkUrl == null || linkName == null)   return;
  Spanned link=Html.fromHtml("<a href=\"" + linkUrl + "\">"+ linkName+ "</a>");
  entryTextView.setText(link);
  entryTextView.setMovementMethod(WPLinkMovementMethod.getInstance());
}
