{
  if (textView == null || linkUrl == null || linkName == null)   return;
  String html="<a href=\"" + linkUrl + "\">"+ linkName+ "</a>";
  textView.setText(Html.fromHtml(html));
  textView.setMovementMethod(WPLinkMovementMethod.getInstance());
}
