{
  if (topics == null || topics.size() != this.size())   return false;
  for (  ReaderTopic topic : topics) {
    if (indexOfTopic(topic.getTopicName()) == -1)     return false;
  }
  return true;
}
