{
  if (originalNote == null || updatedNote == null)   return -1;
  int position=getPosition(originalNote);
  if (position >= 0) {
    remove(originalNote);
    insert(updatedNote,position);
    notifyDataSetChanged();
  }
  return position;
}
