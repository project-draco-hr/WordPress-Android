{
  if (params.length < 1 || mActivity == null || mActivity.getPost() == null) {
    return null;
  }
  String content=StringUtils.notNullStr(params[0]);
  return WPHtml.fromHtml(content,mActivity,mActivity.getPost(),getMaximumThumbnailWidth());
}
