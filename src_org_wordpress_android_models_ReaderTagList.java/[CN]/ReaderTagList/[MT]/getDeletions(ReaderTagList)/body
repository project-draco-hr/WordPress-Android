{
  ReaderTagList deletions=new ReaderTagList();
  if (tags == null)   return deletions;
  for (  ReaderTag thisTag : this) {
    if (tags.indexOfTag(thisTag.getTagName()) == -1)     deletions.add(thisTag);
  }
  return deletions;
}
