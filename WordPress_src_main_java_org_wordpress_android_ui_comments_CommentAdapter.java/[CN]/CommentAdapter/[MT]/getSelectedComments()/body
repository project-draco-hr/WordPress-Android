{
  CommentList comments=new CommentList();
  if (!mEnableSelection)   return comments;
  for (  Integer position : mSelectedPositions) {
    if (isPositionValid(position))     comments.add(mComments.get(position));
  }
  return comments;
}
