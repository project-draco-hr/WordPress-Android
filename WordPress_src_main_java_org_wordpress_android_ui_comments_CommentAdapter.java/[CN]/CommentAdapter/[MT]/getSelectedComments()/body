{
  CommentList comments=new CommentList();
  if (!mEnableSelection) {
    return comments;
  }
  for (  Long commentId : mSelectedCommentsId) {
    if (isCommentIdValid(commentId))     comments.add(mComments.get(indexOfCommentId(commentId)));
  }
  return comments;
}
