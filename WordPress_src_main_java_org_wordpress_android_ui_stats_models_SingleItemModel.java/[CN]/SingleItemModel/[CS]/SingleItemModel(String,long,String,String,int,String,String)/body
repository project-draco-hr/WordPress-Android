{
  this.mBlogID=blogId;
  this.mItemID=itemID;
  this.mTitle=title;
  this.mTotals=totals;
  if (url == null || "null".equals(url) || "false".equals(url)) {
    this.mUrl="";
  }
 else {
    this.mUrl=url;
  }
  this.mDate=date;
  this.mIcon=icon;
}
