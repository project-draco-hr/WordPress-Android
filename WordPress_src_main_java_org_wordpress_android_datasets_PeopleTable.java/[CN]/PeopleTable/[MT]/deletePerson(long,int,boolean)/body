{
  String[] args=new String[]{Long.toString(personID),Integer.toString(localTableBlogId),Integer.toString(isFollower ? 1 : 0)};
  getWritableDb().delete(PEOPLE_TABLE,"person_id=? AND local_blog_id=? AND is_follower=?",args);
}
