{
  String[] args=new String[]{Integer.toString(localTableBlogId),Integer.toString(0)};
  getWritableDb().delete(PEOPLE_TABLE,"local_blog_id=?1 AND is_follower=?2 " + "AND is_email_follower=?2 AND is_viewer=?2",args);
}
