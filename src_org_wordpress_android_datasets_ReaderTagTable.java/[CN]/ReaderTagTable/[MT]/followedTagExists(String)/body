{
  if (TextUtils.isEmpty(tagName)) {
    return false;
  }
  String[] args={tagName,Integer.toString(ReaderTagType.FOLLOWED.toInt())};
  return SqlUtils.boolForQuery(ReaderDatabase.getReadableDb(),"SELECT 1 FROM tbl_tags WHERE tag_name=?1 AND topic_type=?2",args);
}
