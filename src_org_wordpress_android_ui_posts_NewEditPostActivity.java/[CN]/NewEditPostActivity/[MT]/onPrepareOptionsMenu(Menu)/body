{
  super.onPrepareOptionsMenu(menu);
  if (getSupportActionBar() != null)   menu.findItem(R.id.menu_edit_post).setVisible(getSupportActionBar().getNavigationMode() == ActionBar.NAVIGATION_MODE_TABS);
  return true;
}
