{
  String[] args={Long.toString(blogId),Long.toString(postId)};
  return SqlUtils.stringForQuery(ReaderDatabase.getReadableDb(),"SELECT blog_url FROM tbl_posts WHERE blog_id=? AND post_id=?",args);
}
