{
  String sql="SELECT published FROM tbl_posts" + " WHERE feed_id = ?" + " ORDER BY published LIMIT 1";
  return SqlUtils.stringForQuery(ReaderDatabase.getReadableDb(),sql,new String[]{Long.toString(feedId)});
}
