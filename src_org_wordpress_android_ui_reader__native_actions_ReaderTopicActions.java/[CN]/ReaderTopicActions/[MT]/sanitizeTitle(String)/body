{
  if (topicName == null)   return "";
  String sanitized=topicName.replace("&","").replace(" ","-").replace(".","-");
  while (sanitized.contains("--"))   sanitized=sanitized.replace("--","-");
  return sanitized.trim();
}
