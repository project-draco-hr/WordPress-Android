{
  db.execSQL("CREATE TABLE tbl_blog_urls (" + "	blog_url        TEXT COLLATE NOCASE PRIMARY KEY," + " is_followed     INTEGER DEFAULT 0)");
  db.execSQL("CREATE TABLE tbl_recommended_blogs (" + " blog_id         INTEGER DEFAULT 0 PRIMARY KEY," + " follow_reco_id  INTEGER DEFAULT 0,"+ " score           INTEGER DEFAULT 0,"+ "	title           TEXT,"+ "	blog_domain     TEXT COLLATE NOCASE,"+ "	image_url       TEXT,"+ "	reason          TEXT)");
}
