{
  if (isFullScreen == mIsFullScreen || !isFullScreenSupported())   return;
  if (mPost.isWP())   animateIconBar(!isFullScreen);
  if (isFullScreen) {
    getSupportActionBar().hide();
  }
 else {
    getSupportActionBar().show();
  }
  mIsFullScreen=isFullScreen;
}
