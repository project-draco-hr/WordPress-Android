{
  if (isFullScreen == mIsFullScreen)   return;
  if (mPost.isWP())   animateActionBar(!isFullScreen);
  if (isFullScreen) {
    getSupportActionBar().hide();
  }
 else {
    getSupportActionBar().show();
  }
  mIsFullScreen=isFullScreen;
}
