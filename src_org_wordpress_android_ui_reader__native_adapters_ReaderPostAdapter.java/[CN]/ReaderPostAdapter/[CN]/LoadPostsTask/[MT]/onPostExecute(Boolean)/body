{
  if (result) {
    mPosts=(ReaderPostList)(tmpPosts.clone());
    if (mIsImagePreloadingEnabled && mPosts.size() >= PRELOAD_OFFSET) {
      for (int i=1; i <= PRELOAD_OFFSET; i++)       preloadPostImages(i);
    }
    notifyDataSetChanged();
  }
  if (mDataLoadedListener != null)   mDataLoadedListener.onDataLoaded(isEmpty());
  mIsTaskRunning=false;
}
