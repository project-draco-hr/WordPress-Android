{
  Cursor c=getReadableDb().query(AUTOSAVETEXT_TABLE,new String[]{"value"},"key=?",new String[]{key},null,null,null);
  try {
    if (c.moveToFirst()) {
      return c.getString(0);
    }
  }
  finally {
    SqlUtils.closeCursor(c);
  }
  return defaultResult;
}
