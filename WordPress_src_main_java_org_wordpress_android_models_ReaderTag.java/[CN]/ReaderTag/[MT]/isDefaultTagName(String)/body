{
  if (TextUtils.isEmpty(tagName)) {
    return false;
  }
  return (tagName.equalsIgnoreCase(TAG_NAME_FOLLOWED_SITES) || tagName.equalsIgnoreCase(TAG_NAME_FRESHLY_PRESSED) || tagName.equalsIgnoreCase(TAG_NAME_LIKED));
}
