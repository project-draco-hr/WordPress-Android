{
  if (tagName == null) {
    return "";
  }
  if (Character.isUpperCase(tagName.charAt(0))) {
    return tagName;
  }
  if (tagName.length() > 1 && Character.isUpperCase(tagName.charAt(0)) && Character.isLowerCase(tagName.charAt(1))) {
    return tagName;
  }
  return StringUtils.capitalize(tagName);
}
