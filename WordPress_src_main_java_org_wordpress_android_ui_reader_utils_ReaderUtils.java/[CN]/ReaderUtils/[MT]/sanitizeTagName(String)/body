{
  if (tagName == null) {
    return "";
  }
  String sanitized=tagName.trim().replace("&","").replace("#","").replace(" ","-").replace(".","-");
  while (sanitized.contains("--")) {
    sanitized=sanitized.replace("--","-");
  }
  return sanitized.trim();
}
