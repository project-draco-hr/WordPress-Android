{
  if (!isFullScreenSupported() || enableFullScreen == mIsFullScreen) {
    return false;
  }
  if (enableFullScreen) {
    getActionBar().hide();
  }
 else {
    getActionBar().show();
  }
  mIsFullScreen=enableFullScreen;
  return true;
}
