{
  if (TextUtils.isEmpty(blogUrl)) {
    return "";
  }
  int width=DisplayUtils.getDisplayPixelWidth(this);
  return "http://s.wordpress.com/mshots/v1/" + UrlUtils.urlEncode(blogUrl) + "?w="+ Integer.toString(width);
}
