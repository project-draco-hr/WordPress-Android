{
  if (position >= mTextViews.size())   return;
  View tab=mTextViews.get(position);
  LinearLayout parentView=(LinearLayout)tab.getParent();
  scrollToTab(parentView,position);
  setSelectedLayout(parentView);
}
