{
  SQLiteDatabase db=TestUtils.loadDBFromDump(targetContext,testContext,"one_category.sql");
  CategoryNode node=CategoryNode.createCategoryTreeFromDB(1);
  assertTrue((node.getChildren().size() != 0));
  db.close();
}
