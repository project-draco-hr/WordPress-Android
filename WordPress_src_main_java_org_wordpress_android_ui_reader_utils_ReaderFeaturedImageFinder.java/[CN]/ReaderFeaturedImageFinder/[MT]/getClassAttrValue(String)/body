{
  if (tag == null) {
    return null;
  }
  Matcher matcher=CLASS_ATTR_PATTERN.matcher(tag);
  if (matcher.find()) {
    return tag.substring(matcher.start() + 7,matcher.end() - 1);
  }
 else {
    return null;
  }
}
