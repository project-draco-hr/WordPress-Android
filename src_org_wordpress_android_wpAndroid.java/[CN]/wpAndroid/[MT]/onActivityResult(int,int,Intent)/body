{
  super.onActivityResult(requestCode,resultCode,data);
  if (data != null) {
    Bundle extras=data.getExtras();
switch (requestCode) {
case 0:
      displayAccounts();
    break;
}
}
 else {
displayAccounts();
}
}
