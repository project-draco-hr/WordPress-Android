{
  if (response.has("errors")) {
    mErrorObjectsingleCall=response.toString();
    AppLog.e(AppLog.T.STATS,"The single call " + restCallPATH + " failed with the following response: "+ response.toString());
    return true;
  }
  return false;
}
