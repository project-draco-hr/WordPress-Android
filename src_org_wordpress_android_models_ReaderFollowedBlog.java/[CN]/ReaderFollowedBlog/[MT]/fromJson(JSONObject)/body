{
  ReaderFollowedBlog blog=new ReaderFollowedBlog();
  if (json == null) {
    return blog;
  }
  blog.id=json.optLong("ID");
  blog.blogId=json.optLong("blog_ID");
  blog.setUrl(JSONUtil.getString(json,"URL"));
  return blog;
}
