{
  final String siteAddress=siteURL.getText().toString().trim();
  if (siteAddress.equals("")) {
    AlertUtil.showAlert(NewBlogPageFragment.this.getActivity(),R.string.required_fields,R.string.blog_address_required);
    return false;
  }
  return true;
}
