{
  final Handler handler=new Handler();
  mRenderBuilder=new StringBuilder(getPostContent());
  new Thread(){
    @Override public void run(){
      if (!mResourceVars.canShowTiledGallery) {
        resizeImages();
      }
      resizeIframes();
      final String htmlContent=formatPostContentForWebView(mRenderBuilder.toString(),mResourceVars.canShowTiledGallery);
      mRenderBuilder=null;
      handler.post(new Runnable(){
        @Override public void run(){
          renderHtmlContent(htmlContent);
        }
      }
);
    }
  }
.start();
}
