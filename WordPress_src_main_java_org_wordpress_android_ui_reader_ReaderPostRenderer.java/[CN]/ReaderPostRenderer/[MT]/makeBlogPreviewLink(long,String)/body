{
  String label=String.format(WordPress.getContext().getString(R.string.reader_discover_visit_blog),blogName);
  String url="wordpress://blogpreview?blogId=" + Long.toString(blogId);
  return "<a href='" + url + "'>"+ label+ "</a>";
}
