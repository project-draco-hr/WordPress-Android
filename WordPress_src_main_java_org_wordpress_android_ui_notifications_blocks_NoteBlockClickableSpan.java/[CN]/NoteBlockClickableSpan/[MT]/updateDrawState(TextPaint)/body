{
  textPaint.bgColor=mShouldLink && mPressed && !isBlockquoteType() ? mBackgroundColor : Color.TRANSPARENT;
  textPaint.setColor(mShouldLink && !mIsFooter ? mLinkColor : mTextColor);
  textPaint.setUnderlineText(mIsFooter);
}
