{
  if (!loadMore) {
    showProgressBar();
  }
  Vector apiArgs=new Vector();
  apiArgs.add(blog);
  apiArgs.add(isPage);
  apiArgs.add(viewPosts.this);
  new ApiHelper.getRecentPostsTask().execute(apiArgs);
}
