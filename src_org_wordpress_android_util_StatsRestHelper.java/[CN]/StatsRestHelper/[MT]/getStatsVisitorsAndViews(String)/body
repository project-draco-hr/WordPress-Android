{
  getStatsSummary(blogId,new StatsSummaryInterface(){
    @Override public void onSuccess(){
      updateRefreshMap(StatsViewType.VISITORS_AND_VIEWS,-1);
    }
    @Override public void onFailire(){
      updateRefreshMap(StatsViewType.VISITORS_AND_VIEWS,-1);
    }
  }
);
}
