{
  String optionsString=getBlogOptions();
  if (optionsString == null)   return false;
  try {
    JSONObject options=new JSONObject(optionsString);
    if (options.has("jetpack_client_id"))     return true;
  }
 catch (  JSONException e) {
    e.printStackTrace();
  }
  return false;
}
