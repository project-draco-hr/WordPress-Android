{
  if (mNote == null || blockObject == null)   return false;
  if (mNote.isCommentType()) {
    return (TextUtils.isEmpty(JSONUtils.queryJSON(blockObject,"type","")) && mNote.getCommentReplyId() == JSONUtils.queryJSON(blockObject,"ranges[1].id",0));
  }
  return false;
}
