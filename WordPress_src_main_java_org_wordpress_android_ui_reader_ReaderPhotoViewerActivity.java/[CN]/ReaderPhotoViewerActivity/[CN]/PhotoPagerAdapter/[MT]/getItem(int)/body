{
  final String title;
  if (getCount() > 1) {
    title=getString(R.string.reader_title_photo_viewer,position + 1,getCount());
  }
 else {
    title=null;
  }
  return ReaderPhotoViewerFragment.newInstance(mImageList.get(position),mImageList.isPrivate(),title);
}
