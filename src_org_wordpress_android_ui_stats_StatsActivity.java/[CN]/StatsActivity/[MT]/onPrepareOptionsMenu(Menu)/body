{
  if (WordPress.hasValidWPComCredentials(this))   menu.findItem(R.id.menu_view_stats_login).setVisible(false);
 else   menu.findItem(R.id.menu_view_stats_login).setVisible(true);
  return super.onPrepareOptionsMenu(menu);
}
