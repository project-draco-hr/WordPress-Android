{
  if (TextUtils.isEmpty(url)) {
    return 0;
  }
  String normUrl=UrlUtils.normalizeUrl(url);
  String[] args={normUrl,normUrl};
  return SqlUtils.longForQuery(ReaderDatabase.getReadableDb(),"SELECT blog_id FROM tbl_blog_info WHERE feed_url=? OR blog_url=?",args);
}
